const texto = (prompt('Ingrese numero al que desea contar'));


for (let i = 0; i < texto.length; i++) {
    console.log(' --> ' + texto[i]);
}

const conteo = Number(prompt('Ingrese numero al que desea contar'));

for (let i = 0; i <= conteo; i++) {
    console.log(' --> ' +i);
}